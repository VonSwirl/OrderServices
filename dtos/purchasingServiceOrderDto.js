/**
 * purchasingServiceOrderDto.js file pass's a tailored order DTO to the purchasing 
 * service if stock is missing and needs to be ordered before
 * the order can be passed on to the processing service.
 */

 